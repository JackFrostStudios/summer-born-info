---
name: deprecated-automated-test-developer
description: Deprecated compatibility skill kept only to avoid stale references during migration. Do not use this skill for new work; use `api-automated-test-developer` for API tasks or `ui-automated-test-developer` for UI tasks instead.
---

# Deprecated Automated Test Developer

This skill is deprecated.

- For API work, use `api-automated-test-developer`.
- For UI work, use `ui-automated-test-developer`.
- Update any stale references to `automated-test-developer` when you encounter them.
