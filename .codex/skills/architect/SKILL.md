---
name: deprecated-architect
description: Deprecated compatibility skill kept only to avoid stale references during migration. Do not use this skill for new work; use `api-architect` for API tasks or `ui-architect` for UI tasks instead.
---

# Deprecated Architect

This skill is deprecated.

- For API work, use `api-architect`.
- For UI work, use `ui-architect`.
- Update any stale references to `architect` when you encounter them.
